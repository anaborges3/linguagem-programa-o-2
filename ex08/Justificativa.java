package ex08;

public class Justificativa {
    
    /*
     * 
     * Flexibilidade na hierarquia de classes:
     * Interfaces permitem que uma classe implemente múltiplas interfaces, enquanto uma classe abstrata só pode ter uma superclasse. Isso proporciona mais flexibilidade na modelagem da hierarquia de classes, especialmente quando diferentes tipos de comportamento precisam ser agrupados de maneira independente da hierarquia de herança.
     * Encapsulamento mais forte:
     * Interfaces fornecem um nível mais alto de encapsulamento, já que apenas a assinatura dos métodos é declarada nas interfaces, sem a implementação. Isso ajuda a garantir que as classes concretas tenham que fornecer sua própria implementação para cada método da interface, o que promove uma separação mais clara entre interface e implementação.
     * Contratos claros:
     * Interfaces estabelecem contratos claros para as classes que as implementam, especificando quais métodos devem ser implementados. Isso torna o código mais previsível e facilita a compreensão do comportamento esperado de cada classe.
     * Reusabilidade e extensibilidade:
     * Interfaces promovem maior reusabilidade e extensibilidade do código, permitindo que diferentes classes implementem a mesma interface para compartilhar um conjunto comum de comportamentos. Isso é especialmente útil em sistemas complexos onde várias classes podem ter comportamentos semelhantes, mas com implementações específicas.
     * Desacoplamento:
     * Usar interfaces em vez de classes abstratas promove um menor acoplamento entre componentes do sistema, já que as classes não precisam estar vinculadas a uma hierarquia específica de classes. Isso facilita a manutenção e evolução do código ao longo do tempo, já que mudanças em uma interface não afetam diretamente as classes que a implementam.
     */
}
